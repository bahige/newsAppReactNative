import React from 'react'
import { ScrollView, Text } from 'react-native'

const Headlines = () => {
    return (
        <ScrollView>
            <Text>This is the Headlines Component</Text>
        </ScrollView>
    )
}

export default Headlines
